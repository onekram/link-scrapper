app:
  telegram-token: "mock_token"
  scrapper-url: "http://localhost:8083"
  telegram-url: "http://localhost:8083"

features:
  webhook:
    enabled: false

spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  data:
    redis:
      host: localhost
      port: 6379
      password: "redisredis"

springdoc:
  swagger-ui:
    enabled: false
