features:
  webhook:
    enabled: false

logging:
  level:
    root: info

spring:
  application:
    name: Bot
  liquibase:
    enabled: false
  jackson:
    deserialization:
      fail-on-unknown-properties: true

server:
  port: 8080
